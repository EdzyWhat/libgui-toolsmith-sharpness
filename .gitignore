# .NET build output
bin/
obj/

# Release artifacts (regenerated by build/release.sh)
dist/
[Rr]elease/
[Rr]eleases/
*.user
*.suo

# IDEs
.vs/
.idea/
.vscode/
*.DS_Store

# Claude Code local worktrees / machine-local settings
.claude/worktrees/
.claude/settings.local.json

# Vintage Story runtime data (never commit game data or the game itself)
VintagestoryData/

# Vendored reference DLLs - other mods' compiled assemblies, not ours to redistribute.
# Kept locally for compile-time reference only; obtain per lib/README.md.
lib/*.dll
